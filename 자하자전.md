# 慈河字典 · 자하자전

한국·중국 금석문 **탁본(拓本) 서예 집자(集字) 참고 도구**. 비석 글자의 탁본 이미지를 글자별로 찾아보고, 여러 글자를 모아 서예지에 배치하고, PNG로 내보내는 단일 HTML 애플리케이션입니다. 데스크탑과 모바일 UI를 모두 지원합니다.

현재 기본 수록: **道因法師碑**(구양통 해서 · 당). `manifest.json`에 폴더명을 추가하면 다른 서체 사전을 얼마든지 붙일 수 있습니다.

---

## 폴더 구조

```
main/
├ 자하자전.html          # 애플리케이션 (단일 파일)
├ manifest.json          # 서체 폴더 목록
├ 도인법사비/
│  ├ DB.json             # 글자·위치·페이지 데이터
│  ├ page-1/
│  │  ├ 1-1-1.png        # page-열-행.png
│  │  ├ 1-1-2.png
│  │  └ …
│  ├ page-2/
│  └ …
└ 구성궁예천명/           # (서체 추가 예시)
   ├ DB.json
   └ page-1/ …
```

`자하자전.html`은 각 서체 폴더의 `DB.json`과 탁본 이미지를 **상대경로로 참조**합니다.

---

## 빠른 시작

`fetch()`로 데이터를 불러오기 때문에 **로컬 서버**에서 열어야 합니다. `file://`로 바로 열면 브라우저 보안정책(CORS)에 막혀 데이터가 안 뜹니다.

```bash
cd main
python -m http.server 8000
# 브라우저에서 http://localhost:8000/자하자전.html
```

VS Code의 Live Server 확장 등 어떤 정적 서버든 무방합니다.

---

## 서체 추가하기 (manifest.json)

`manifest.json`은 **서체 폴더 목록**입니다. 폴더명만 한 줄 추가하면 탭이 자동으로 생깁니다.

```json
["도인법사비", "구성궁예천명"]
```

각 폴더의 `DB.json` `metadata.title`·`metadata.sub`를 읽어 **탭 이름과 제목이 자동으로 채워집니다**(하드코딩 없음). 첫 서체는 즉시 로드되고, 나머지는 백그라운드로 메타데이터만 읽어 탭 이름을 채웁니다. 한 번 연 서체는 캐시되어 탭 전환이 즉시입니다.

메타데이터 대신 라벨을 직접 고정하고 싶으면 객체로 적을 수도 있습니다.

```json
[
  "도인법사비",
  { "folder": "구성궁예천명", "title": "九成宮醴泉銘", "sub": "구양순 · 구성궁" }
]
```

`manifest.json`이 없거나 비어 있으면 `도인법사비` 폴더로 자동 폴백합니다.

---

## DB.json 스키마

```jsonc
{
  "metadata": {
    "title": "道因法師碑",          // 탭·제목에 표시
    "sub": "구양통 · 도인법사비",    // 부제
    "baseImagePath": "ref",         // DB 생성 프로그램 전용. 앱은 무시함
    "generated": "2026-06-01T…"
  },

  // 글자별 정보 (모든 필드 선택적)
  "characters": {
    "道": {
      "korean":     ["길 도"],       // 훈독
      "radical":    "辵",            // 부수
      "components": ["首", "辵"],    // 구성요소
      "variants":   ["衜"],          // 이체자 (탭하면 해당 글자로 검색)
      "memo":       "…"              // 자유 메모
    }
  },

  // 글자별 출현 위치 → 탁본 이미지를 가리킴
  "positions": {
    "道": [
      [page, col, row, variant]      // 예: [1, 2, 5, 0]  (variant 0 = 기본)
    ]
  },

  // 페이지(면) 구성
  "pages": {
    "general":    { "cols": 5, "rows": 8 },   // 한 면의 열·행 수
    "totalPages": 12,
    "1": { "cells": { "1,1": "道", "2,1": "因" } }  // "열,행": 글자
  }
}
```

### 이미지 경로 규칙

```
./{폴더명}/page-{page}/{page}-{col}-{row}.png
예) ./도인법사비/page-1/1-1-1.png
```

`positions`의 `[page, col, row]`가 그대로 파일명이 됩니다. 페이지 보기는 **우철(오른쪽→왼쪽)** 로 배열됩니다.

---

## 기능

### 검색
- **한자**: 글자를 입력해 그 글자의 모든 탁본 인스턴스를 카드로 표시. 여러 글자를 넣으면 자동으로 集字로 넘어감.
- **뜻·부수**: 훈독·부수·구성요소·이체자·메모를 대상으로 실시간 검색(검색 범위 칩으로 조정).

### 글자 상세
큰 탁본 이미지, 훈독, 부수·구성요소, 이체자(탭하면 재검색), 메모, **전체 출현 인스턴스 격자**(탭해 다른 탁본으로 교체).

### 集字 (집자)
여러 글자를 모아 구성. 글자마다 인스턴스(‹ n/N ›)를 바꿀 수 있고, 편집기로 넘겨 배치하거나 바로 PNG로 저장.

### 集字 편집기
서예지 캔버스 위에 글자를 자유 배치.
- **이동**: 글자 드래그 · **크기**: 모서리 핸들 드래그(데스크탑) / 두 손가락 핀치(모바일)
- **화면 이동·확대**: Space+드래그 또는 두 손가락 팬 / 스크롤·핀치 줌
- **그리드**: 칸 크기·간격·색·투명도·스냅 설정
- **가이드 선**: `╱ 선` → `가로`/`세로` 선택 후 클릭하면 그 위치에 전체 선 생성. **이분선** 체크 후 드래그(a→b)하면 **a·b의 중점을 지나는 전체 가로/세로선** 생성. 선은 선택·이동·삭제 가능하며 PNG에도 출력됨.
- **글자 자르기(✂)**: 글자를 선택하고 ✂ → 탁본에서 원하는 영역만 드래그로 잘라 다시 배치(모바일 터치 지원).
- **삭제**: 선택 후 `Delete`/`Backspace` 또는 🗑
- **PNG 저장**: 고해상도로 내보내기

### 페이지 보기
면 단위로 탁본 전체를 우철 격자로 열람. 칸을 탭하면 글자 상세로 이동.

### 기타
- 라이트/다크 테마, 로컬 `DB.json` 수동 불러오기(폴백)
- **모바일 UI**: 하단 탭(검색 · 集字 · 頁), 전체화면 글자 상세, 안드로이드 뒤로가기 연동

---

## 단축키 (데스크탑)

| 구분 | 동작 | 키 |
|---|---|---|
| 검색 | 검색창 포커스 | `/` |
| 검색 | 검색 초기화 | `Escape` |
| 카드 | 이전 / 다음 카드 | `Alt+←` `Alt+→` |
| 集字 | 패널 열기/닫기 | `Alt+J` |
| 集字 | 인스턴스 전환 | `[` `]` |
| 集字 | 이미지 저장 | `Alt+S` |
| 集字 | 편집기 열기/닫기 | `Alt+E` |
| 편집기 | 선택 삭제 | `Delete` / `Backspace` |
| 페이지 | 패널 열기/닫기 | `Alt+P` |
| 페이지 | 이전 / 다음 | `Alt+,` `Alt+.` |
| 기타 | 테마 전환 | `Shift+T` |
| 기타 | 도움말 | `/` (검색창 밖) |

---

## 데이터 준비 메모
- `positions`의 좌표가 실제 이미지 파일명과 정확히 일치해야 탁본이 표시됩니다.
- `characters` 항목이 없어도 `positions`만 있으면 검색·표시는 됩니다(상세 정보만 비어 있음).
- 이체자(`variants`)에 넣은 글자도 별도 `positions`/이미지가 있으면 탭해서 바로 열람할 수 있습니다.
